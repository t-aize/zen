# Bot token — Discord Developer Portal → your app → Bot → Token
DISCORD_TOKEN=

# Database URL — for example, "file:../../local.db"
DATABASE_URL=file:../../local.db

# "development" | "production"  (defaults to "development" if omitted)
NODE_ENV=development

